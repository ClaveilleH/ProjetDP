\documentclass{rapportClass} % Classe de document (rapport)



\title{Rapport de Projet}
\author{Hugo CLAVEILLE}
% -- Master 1 Informatique
\date{\today}

% oarsub -l /host=1,gpu=2,walltime=4:00:00 -t besteffort ./run_gpu.sh
% oarstat -j 2171972
% scp hclaveil@access.grid5000.fr:/path/to/file /local/path



% Appliquer aussi aux pages de type 'plain' (début de chapitre, sommaire…)
% \fancypagestyle{plain}{
%   \fancyhf{}
%   \fancyhead[L]{Rapport de Projet}
%   \fancyhead[C]{M1 Informatique}
%   \fancyhead[R]{Hugo}
%   \fancyfoot[C]{\thepage}
%   \renewcommand{\headrulewidth}{0.4pt}
% }

\begin{document}

\begin{titlepage}
    \thispagestyle{empty} % pas d'en-tête/pied ici
    
    \begin{center}
        % Logo en haut
        \includegraphics[width=1\textwidth]{logo.png}\\[2cm]
        
        % Titre
        {\Huge \textbf{Rapport de Projet}}\\[1cm]
        {\Large Distributed Memory}\\[1cm]

        \vspace{1cm}
        % Auteur
        \textbf{Hugo CLAVEILLE}\\[0.5cm]
        
        % Sous-titre
        {\Large Master 1 Informatique}\\[2cm]
        
        % Date
        \vfill
        {\large \today}
    \end{center}
\end{titlepage}

\newpage
\thispagestyle{empty}
\tableofcontents
\newpage

% ======================================================================================================================
Introduction of the cluster Grid5000 and node tested (2 points) 
*Introduction of the task (1 point) 
Evaluation (17 points) 
    Details of the node tested (2 points) 
    Details of the training model tested (1 point) 
    Impact of the number of computing devices (8 points) 
    Impact of the batch size (6 points) 
        Test additional batch size 16,64,128 (GPU node) 
        Three plots (for throughput) (3 points) 
        One plot shows the optimal number of devices for different batch sizes (1 point) 
        Analyses (2 points)
% ======================================================================================================================
\chapter{Introduction}
Dans le cadre du projet de dynamique programing il nous a été demandé de réaliser une étude sur l'impact du nombre de ressources de calcul
(CPU/GPU) et de la taille du batch sur les performances d'entraînement d'un modèle de deep learning. 
Pour cela nous avons utilisé le cluster Grid5000 et plus particulièrement le noeud \textit{sophia} qui est équipé de GPU.

% \section{Introduction a la tache}


% ======================================================================================================================
\chapter{Evaluation}
\section{Details du noeud testé} %VGG19\_BN
% \input{rapport/details_noeud}

\section{Details du modele d'entraînement testé}
Le modèle utilisé dans ce projet est VGG19 avec Batch Normalization (VGG19BN),
issu de l’architecture VGG proposée dans l’article “Very Deep Convolutional Networks for Large-Scale Image Recognition”. 
Il s’agit d’un réseau de neurones convolutif profond composé de 19 couches avec paramètres, 
incluant 16 couches convolutives et 3 couches entièrement connectées.

La variante VGG19\_BN intègre des couches de Batch Normalization après chaque convolution, ce qui permet de stabiliser l’entraînement, 
d’accélérer la convergence et d’améliorer l’utilisation des ressources de calcul. Le modèle est implémenté à l’aide 
de la bibliothèque torchvision via la fonction torchvision.models.VGG19\_BN.

VGG19\_BN est un modèle particulièrement exigeant en ressources, avec environ 143 millions de paramètres et un coût de calcul 
d’environ 19,6 GFLOPS, ce qui en fait un candidat pertinent pour l’évaluation des performances et de la scalabilité sur des nœuds GPU. 
Sa complexité permet d’observer clairement l’impact du nombre de dispositifs de calcul et de la taille des batchs sur le débit d’entraînement 
(throughput).


\section{Impact du nombre de CPU/GPU}
Afin d'évaluer l'impact du nombre de ressources de calcul sur les performances d'entraînement, 
j'ai réalisé plusieurs tests en faisant varier le nombre de CPU et de GPU utilisés.
J'ai aussi modifié le fichier \textit{demo.py} pour enregistrer les temps de chargement des données et les temps de calculs dans des fichiers csv.
Enfin, j'ai utilisé des scripts python pour traiter les résultats et générer la données nécessaires aux graphiques.
% \input{rapport/impact_devices}
\subsection{Impact du nombre de CPU}
\begin{figure}[H]
    \centering
    \begin{tikzpicture}
    \begin{axis}[
        xtick distance=1,            % pas des graduations = 1
        /pgf/number format/fixed,    % format fixe
        /pgf/number format/precision=0, % 0 décimales -> entiers
        width=1\textwidth,
        height=0.5\textwidth,
        xlabel={Nombre de CPU},
        ylabel={Time (s)},
        legend pos=north east,
        grid=major,
    ]
    % TOTAL TIME
    \addplot[
        color=blue,
        mark=square,
    ]
    table[
        x=nb_cpu,
        y=total_time,
        col sep=semicolon
    ] {../results/assets/fig1.csv};
    \addlegendentry{Total Time}
    
    % COMPUTING TIME
    \addplot[
        color=red,
        mark=triangle,
    ]
    table[
        x=nb_cpu,
        y=computing_time,
        col sep=semicolon
    ] {../results/assets/fig1.csv};
    \addlegendentry{Computing Time}
    
    % LOADING TIME
    \addplot[
        color=green,
        mark=o,
    ]
    table[
        x=nb_cpu,
        y=loading_time,
        col sep=semicolon
    ] {../results/assets/fig1.csv};
    \addlegendentry{Loading Time}   
    \end{axis}
    \end{tikzpicture}
    \caption{Impact du nombre de CPU sur les temps d'entraînement.}
    % \label{fig:impact_cpu}
\end{figure}

On peut donc observer que sur un batch de taille donnée (32 ici), l'augmentation du nombre de CPU ne permet pas de réduire le temps d'entraînement.


\subsection{Impact du nombre de GPU}
\begin{figure}[H]
    \centering
    \begin{tikzpicture}
    \begin{axis}[
        xtick distance=1,            % pas des graduations = 1
        /pgf/number format/fixed,    % format fixe
        /pgf/number format/precision=0, % 0 décimales -> entiers
        width=1\textwidth,
        height=0.5\textwidth,
        xlabel={Nombre de CPU},
        ylabel={Time (s)},
        legend pos=north east,
        grid=major,
    ]
    % TOTAL TIME
    \addplot[
        color=blue,
        mark=square,
    ]
    table[
        x=nb_cpu,
        y=total_time,
        col sep=semicolon
    ] {../results/assets/fig2.csv};
    \addlegendentry{Total Time}
    
    % COMPUTING TIME
    \addplot[
        color=red,
        mark=triangle,
    ]
    table[
        x=nb_cpu,
        y=computing_time,
        col sep=semicolon
    ] {../results/assets/fig2.csv};
    \addlegendentry{Computing Time}
    
    % LOADING TIME
    \addplot[
        color=green,
        mark=o,
    ]
    table[
        x=nb_cpu,
        y=loading_time,
        col sep=semicolon
    ] {../results/assets/fig2.csv};
    \addlegendentry{Loading Time}   
    \end{axis}
    \end{tikzpicture}
    \caption{Impact du nombre de GPU sur les temps d'entraînement.}
    % \label{fig:impact_cpu}
\end{figure}
On peut observer que l'augmentation du nombre de GPU permet de réduire légèrement le temps d'entraînement,
mais l'impact reste limité sur ce modèle et cette configuration. A noter que le temps de chargement a partir de 4 GPU augmente significativement.
\section{Impact de la taille du batch}
    % Test additional batch size 16,64,128 (GPU node)
    % Three plots (for throughput) (3 points)
    % One plot shows the optimal number of devices for different batch sizes (1 point)
    % Analyses (2 points)
\begin{figure}[H]
    \centering
    \begin{tikzpicture}
    \begin{axis}[
        % xtick distance=1,            % pas des graduations = 1
        % /pgf/number format/fixed,    % format fixe
        % /pgf/number format/precision=0, % 0 décimales -> entiers
        xtick={16,32,64,128,256},
        width=1\textwidth,
        height=0.5\textwidth,
        xlabel={Batch Size},
        ylabel={Time (s)},
        legend pos=north west,
        grid=major,
    ]
    
    % Courbe pour 1 CPU
    \addplot[
        color=blue,
        mark=square,
    ]
    table[
        x=batch_size,
        y=total_time,
        col sep=semicolon,
        restrict expr to domain={\thisrow{size}}{1:1}  % Filtre pour size=1 (remplacer par le nb de CPU voulu)
    ] {../results/CPU/all.csv};
    \addlegendentry{1 CPU}
    
    % Courbe pour 2 CPU
    \addplot[
        color=red,
        mark=triangle,
    ]
    table[
        x=batch_size,
        y=total_time,
        col sep=semicolon,
        restrict expr to domain={\thisrow{size}}{2:2}  % Filtre pour size=2
    ] {../results/CPU/all.csv};
    \addlegendentry{2 CPU}
    
    % Courbe pour 3 CPU
    \addplot[
        color=green,
        mark=o,
    ]
    table[
        x=batch_size,
        y=total_time,
        col sep=semicolon,
        restrict expr to domain={\thisrow{size}}{3:3}  % Filtre pour size=4
    ] {../results/CPU/all.csv};
    \addlegendentry{3 CPU}
    
    % Courbe pour 4 CPU
    \addplot[
        color=orange,
        mark=diamond,
    ]
    table[
        x=batch_size,
        y=total_time,
        col sep=semicolon,
        restrict expr to domain={\thisrow{size}}{4:4}  % Filtre pour size=8
    ] {../results/CPU/all.csv};
    \addlegendentry{4 CPU}
    
    % Courbe pour 5 CPU
    \addplot[
        color=purple,
        mark=star,
    ]
    table[
        x=batch_size,
        y=total_time,
        col sep=semicolon,
        restrict expr to domain={\thisrow{size}}{5:5}  % Filtre pour size=16
    ] {../results/CPU/all.csv};
    \addlegendentry{5 CPU}

    % Courbe pour 6 CPU
    \addplot[
        color=brown,
        mark=oplus,
    ]
    table[
        x=batch_size,
        y=total_time,
        col sep=semicolon,
        restrict expr to domain={\thisrow{size}}{6:6}  % Filtre pour size=32
    ] {../results/CPU/all.csv};
    \addlegendentry{6 CPU}


    \end{axis}
    \end{tikzpicture}
    \caption{Impact du batch size sur le temps total d'entraînement pour différents nombres de CPU.}
    % \label{fig:impact_batchsize}
\end{figure}
On peut donc voir que pour quelque soit la taille de batch, l'augmentation du nombre de CPU n'est pas plus efficace qu'avec un seul CPU.
Mais qu'à partir du moment ou il y a plusieurs CPU, plus le nombre de CPU est grand, plus le temps d'entraînement est réduit.
On peut aussi observer que l'augmentation de la taille du batch augmente le temps d'entraînement total.
Nous allons maintenant visualiser quel est la raison de cette augmentation du temps d'entraînement en regardant les temps de chargement et de calculs séparément.
\begin{figure}[H]
    \centering
    \begin{tikzpicture}
    \begin{axis}[
        % xtick distance=1,            % pas des graduations = 1
        % /pgf/number format/fixed,    % format fixe
        % /pgf/number format/precision=0, % 0 décimales -> entiers
        xtick={16,32,64,128,256},
        width=1\textwidth,
        height=0.5\textwidth,
        xlabel={Batch Size},
        ylabel={Time (s)},
        legend pos=north west,
        grid=major,
    ]
    % Courbe pour 1 CPU
    \addplot[
        color=blue,
        mark=square,
    ]
    table[
        x=batch_size,
        y=total_time,
        col sep=semicolon,
        restrict expr to domain={\thisrow{size}}{1:1}  % Filtre pour size=1 (remplacer par le nb de CPU voulu)
    ] {../results/CPU/all.csv};
    \addlegendentry{Total 1 CPU}
    % Courbe pour 1 CPU - Loading time
    % en pointillés
    \addplot[
        color=blue,
        mark=square,
        dashed
    ]
    table[
        x=batch_size,
        y=total_loading_time,
        col sep=semicolon,
        restrict expr to domain={\thisrow{size}}{1:1}  % Filtre pour size=1 (remplacer par le nb de CPU voulu)
    ] {../results/CPU/all.csv};
    \addlegendentry{Loading 1 CPU}
    % Courbe pour 1 CPU - Computing time
    
    \addplot[
        color=blue,
        mark=square,
        dashdotted
    ]
    table[
        x=batch_size,
        y=total_computing_time,
        col sep=semicolon,
        restrict expr to domain={\thisrow{size}}{1:1}  % Filtre pour size=1 (remplacer par le nb de CPU voulu)
    ] {../results/CPU/all.csv};
    \addlegendentry{Computing 1 CPU}

    
    % Courbe pour 3 CPU
    \addplot[
        color=red,
        mark=square,
    ]
    table[
        x=batch_size,
        y=total_time,
        col sep=semicolon,
        restrict expr to domain={\thisrow{size}}{3:3}  % Filtre pour size=3
    ] {../results/CPU/all.csv};
    \addlegendentry{Total 3 CPU}
    % Courbe pour 3 CPU - Loading time
    % en pointillés
    \addplot[
        color=red,
        mark=square,
        dashed
    ]
    table[
        x=batch_size,
        y=total_loading_time,
        col sep=semicolon,
        restrict expr to domain={\thisrow{size}}{3:3}  % Filtre pour size=3
    ] {../results/CPU/all.csv};
    \addlegendentry{Loading 3 CPU}
    % Courbe pour 3 CPU - Computing time
    \addplot[
        color=red,
        mark=square,
        dashdotted
    ]
    table[
        x=batch_size,
        y=total_computing_time,
        col sep=semicolon,
        restrict expr to domain={\thisrow{size}}{3:3}  % Filtre pour size=3
    ] {../results/CPU/all.csv};
    \addlegendentry{Computing 3 CPU}
    \end{axis}
    \end{tikzpicture}
    \caption{Impact du batch size sur les temps de chargement et de calculs pour 1 et 3 CPU.}
    % \label{fig:impact_batchsize}
\end{figure}
On peut observer que l'augmentation de la taille du batch augmente principalement le temps de calculs


\end{document}
